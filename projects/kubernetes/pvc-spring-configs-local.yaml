---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-spring-configs-local
spec:
  storageClassName: spring-demo
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 25Mi